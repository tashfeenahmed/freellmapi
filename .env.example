# Server encryption key for API key storage (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=your-64-char-hex-key-here

# Iron-session: seal key for the admin UI cookie (min 32 characters)
SESSION_PASSWORD=replace-with-a-secret-at-least-32-chars

# Server port (default: 3001)
PORT=3001
