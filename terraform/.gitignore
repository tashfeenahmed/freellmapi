# Local state & secrets — never commit
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
terraform.tfvars
*.auto.tfvars
crash.log

# rendered cloud-init (contains ENCRYPTION_KEY)
*.rendered.sh

# exported CA trust bundle (generated per-deployment)
*.pem
